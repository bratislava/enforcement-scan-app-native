on:
  push:
    tags:
      - 'prod[0-9]+.[0-9]+.[0-9]+'
      - 'staging[0-9]+.[0-9]+.[0-9]+'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🚂 Setup repo
        uses: actions/checkout@v4

      - name: 🚛 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: yarn

      - name: 🚧 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_BUILD_TOKEN }}

      - name: 📦 Install dependencies
        run: yarn install

      - name: 🚀 Build production
        if: contains(github.event.ref, 'prod')
        run: |
          if [[ ${{ github.event.ref }} == *"ota"* ]]; then
            eas update --message "${{ github.event.ref }} ${{ github.event.head_commit.message }}" --branch preview --platform android --non-interactive
          else
            eas build --profile prod --platform android --auto-submit --non-interactive
            eas build --profile prod-apk --platform android --non-interactive
          fi

      - name: 🚀 Build staging
        if: contains(github.event.ref, 'staging')
        run: eas build --profile staging --platform android --auto-submit --non-interactive
